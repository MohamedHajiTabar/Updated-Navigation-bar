import { SearchBar } from ".";

export default {
  title: "Components/SearchBar",
  component: SearchBar,
};

export const Default = {
  args: {
    text: "Search",
    focused: false,
    className: {},
    inputType: "text",
  },
};
